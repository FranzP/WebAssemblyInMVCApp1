@using Radzen.Blazor;
@using WebAssemblyInMVCApp1.Views.Shared.Components;
@model Item;

@{
    ViewData["Title"] = "Component Example - RenderMode.ServerPrerendered";
}

<h1>@ViewData["Title"]</h1>

@await(
    Html.RenderComponentAsync<WebAssemblyInMVCApp1.Views.Shared.Components.ItemComponent>(RenderMode.ServerPrerendered, new { ItemDetails = Model })
)

<component type="typeof(BButton)" render-mode="ServerPrerendered" />

@*@await(
    Html.RenderComponentAsync<BButton>(RenderMode.ServerPrerendered)
)*@



@section Scripts {
    <base href="~/" />
    <script src="_framework/blazor.server.js"></script>
}